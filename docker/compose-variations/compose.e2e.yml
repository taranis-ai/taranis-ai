services:
  summary_bot:
    image: ${DOCKER_IMAGE_NAMESPACE}/taranis-core:${TARANIS_TAG:-latest}
    command: ["true"]

  nlp_bot:
    image: ${DOCKER_IMAGE_NAMESPACE}/taranis-core:${TARANIS_TAG:-latest}
    command: ["true"]

  story_bot:
    image: ${DOCKER_IMAGE_NAMESPACE}/taranis-core:${TARANIS_TAG:-latest}
    command: ["true"]

  sentiment_analysis_bot:
    image: ${DOCKER_IMAGE_NAMESPACE}/taranis-core:${TARANIS_TAG:-latest}
    command: ["true"]

  scheduler:
    image: ${DOCKER_IMAGE_NAMESPACE}/taranis-core:${TARANIS_TAG:-latest}
    command: ["true"]
